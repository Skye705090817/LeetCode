
//第一次在LeetCode上刷题，完全不知道该怎么写，找了一个最简单的汉明距离还弄了好久，不过最终通过了，开心。

class Solution {
public:
    int hammingDistance(int x, int y) {

        int res = 0, s = 0;
        
        for (; x > 0 && y > 0; x /= 2, y /= 2) {
            if ((x % 2) == (y % 2))
                res++;
        }
        s = x > 0 ? x : y;
        for(; s > 0; s /= 2) {
            res++;
        }
        return res;
    } 
};
